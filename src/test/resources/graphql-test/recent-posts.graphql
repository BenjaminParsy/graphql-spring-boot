query recentPosts($count: Int!, $offset: Int!) {
    recentPosts(count: $count, offset: $offset) {
        id
        title
        text
        category
        author {
            id
            firstname
            lastname
        }
    }
}