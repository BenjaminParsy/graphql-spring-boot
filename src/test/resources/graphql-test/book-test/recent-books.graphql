query recentBooks($count: Int!, $offset: Int!) {
    recentBooks(count: $count, offset: $offset) {
        id
        title
        text
        category
        author {
            id
            firstname
            lastname
        }
    }
}