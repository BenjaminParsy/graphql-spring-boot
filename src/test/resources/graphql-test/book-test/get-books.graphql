query getBooks($limit: Int!, $offset: Int!) {
    getBooks(limit: $limit, offset: $offset) {
        id
        title
        text
        category
        author {
            id
            firstname
            lastname
        }
    }
}