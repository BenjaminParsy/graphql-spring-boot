mutation createPost($title: String!, $text: String!, $category: String!, $authorId: Int!) {
    createPost(title: $title, text: $text, category: $category, authorId: $authorId) {
        id
        title
        text
        category
        author {
            id
            firstname
            lastname
        }
    }
}